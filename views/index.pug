extends layout

block content
  h1= title
  p Welcome to #{title}
  form(id="new", action="/new", method="post")
    textarea(form="new", rows="10", cols="40")
    input(type="text")
    button(type="submit")